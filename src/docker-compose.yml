volumes:
  wordpress:
  database:

services:
  mariadb:
    build: mariadb/
    image: mariadb_image
    volumes:
      - database:/var/lib/mysql
  wordpress-php:
    build: wordpress-php/
    image: wordpress-php_image
    volumes:
      - wordpress:/var/www/wordpress
    env_file:
      - .env
  nginx:
    build: nginx/
    image: nginx_image
    volumes:
      - wordpress:/var/www/wordpress
    ports:
      - "${HOST_PORT}:443"